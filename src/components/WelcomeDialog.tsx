'use client';

/**
 * Welcome auto-modal disabled while Studio lands on home job cards.
 * Auto-opening a Dialog over the home shell caused click-dead / collapsed-paper
 * states during Fast Refresh. Teaching copy lives in Help + the Teach job card.
 *
 * Keep this stub mounted from Providers so imports stay stable.
 */
export function WelcomeDialog() {
  return null;
}
